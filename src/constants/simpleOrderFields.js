// ⚠️ UNCONFIRMED: No simple order fields were found in the existing codebase (index.html / 200.html).
// The minimal field set below comes from the master build prompt (Section 5.4).
// Verify field names and any additional fields with the shop before deploying this order type.

export const SIMPLE_ORDER_FIELDS = [
  // اسم الصنف — free text (no dropdown options needed)
  // الكمية — numeric
  // ملاحظات — textarea
  // All field definitions are inline in SimpleOrderBody.jsx
];

// Placeholder marker — remove when confirmed by shop
export const SIMPLE_ORDER_CONFIRMED = false;
